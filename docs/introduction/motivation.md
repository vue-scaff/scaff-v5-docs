# 设计初衷

Scaff 希望把应用中反复出现的注册关系，收敛到稳定的目录结构中：页面目录放页面、组件、数仓、国际化、Service 或任意自定义资源，构建阶段统一发现，运行阶段按资源组加载。

v5 将这套理念拆成框架无关 Core、构建工具适配器和 UI 运行时适配器。这样，目录协议和资源生命周期不必绑定 Vue，也可以逐步接入 React 或其他运行时。

“自动”在这里有明确边界：Scaff 自动发现符合规则的文件，并在资源组激活后加载和暴露它们；当前并不会自动理解第三方路由、状态管理或国际化库。
